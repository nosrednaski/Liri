// Get that healthy enviro setup
require("dotenv").config();
const keys = require("keys-js");

var spotify = new Spotify(keys.spotify);
var client = new Twitter(keys.twitter);

// Twitter Functions
var Twitter = require('twitter')
function my-tweets() {

}